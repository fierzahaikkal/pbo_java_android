<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/biruMudaPalette</item>
        <item name="colorPrimaryDark">@color/biruGelapPalette</item>
        <item name="colorAccent">@color/biruTerangPalette</item>
        <item name="android:fontFamily">@font/inter</item>
    </style>

    <style name="AppTheme.NoActionBar"  parent="Theme.Material3.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.MaterialComponents" parent="Theme.MaterialComponents.Light"/>
    <style name="AppTheme.AppCompat" parent="Theme.AppCompat.DayNight.DarkActionBar"/>


</resources>
